# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Application will use this key server-side to analyze resumes
# Users will NOT see or need to enter their own API key
VITE_CHATBASE_KEY=6b7a25de-305d-4c47-9f4e-91dbe278e37f
# Optional iframe host (placeholder) — this example domain is NOT real and will DNS-resolve fail.
# If you have a hosted chat widget you control, put its URL here. Example (replace with your host):
# VITE_CHATBASE_IFRAME_URL=https://your-real-chat-host.example.com/widget
# Recommended: native API mode (do not expose private keys in the browser for production).
# Provide the Chatbase REST endpoint below (the widget will POST { message } to this URL):
VITE_CHATBASE_API_URL=https://api.chatbase.co/v1/chat